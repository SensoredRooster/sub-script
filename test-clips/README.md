# test-clips

Drop sample VODs / replay exports here for local pipeline testing.

```bash
python -m subscript --source test-clips/your-file.mp4
```

Video files (`*.mp4`, `*.mov`, `*.mkv`) are gitignored — only this folder structure is in the repo. Keep clips on your machine (or OneDrive); do not commit large recordings.
